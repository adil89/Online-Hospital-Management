<!DOCTYPE html>
<html>
<head>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>
</head>
<body>



<script type="text/javaScript">

$( document ).ready(function() {
 
 

   // var output =  keywordusage('Dive Into Python is a free book for experienced programmers',['Python', 'python', 'scala']);
   // alert(output);

   // var output2 =  frequencies('foo foo bar foo   bar buz',['foo', 'bar','buz']);
   // alert(output2);


});

function keywordusage(var string, var arr) {
          var ret;
          var myResult = string.split("");
          for (var j = 0; j < arr.length; j  ) {
              for (var i = 0; i < myResult.length; i  ) {
                var result = myResult[i].match(arr[j]);
                if(result)
                  ret[j]='true';
                else
                  ret[j]='false';
              }
            }
            return ret;
}



function frequencies( var string2, var arr2) {
  var count;
          var myResult = string2.split("");
          for (var i = 0; i < arr2.length; i  ) {
            for (var j = 0; j < myResult.length; j  ) {
              var result = myResult[j].match(arr2[i]);
              if(result)
              count  ;
            }
          }
}

</script>

</body>
</html>